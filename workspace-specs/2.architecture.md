# Architecture — places workspace

Decisions locked for the **places** product family. Requirements for the agent: [`1.req-specs.md`](./1.req-specs.md).  
User stories (agent tools + operator admin app): [`../1.places-agent/agent-specs/agent-stories.md`](../1.places-agent/agent-specs/agent-stories.md).  
Knowledge (consolidated): [`knowledge/handbook.md`](./knowledge/handbook.md). Provider probe matrix: [`knowledge/maps/places-capabilities.md`](./knowledge/maps/places-capabilities.md).

**Workspace root:** `~/code/places-workspace/` (umbrella specs). Product domains such as `what2eat.food` remain app hostnames, not the parent folder name.

---

## 1. Purpose & scope

This document defines system structure, trust boundaries, provider gateway behavior, deploy shape, and workspace layout. It does not replace product requirement docs for what2eat or where2play.

**In scope:** three deployables; places-agent core (tools + **operator management web** on the same host); caller-driven providers; result provenance; itinerary ownership; Option 1 deploy on 野草云3; umbrella git.

**Out of scope here:** pixel-level UI, per-app i18n catalogs, detailed OpenAPI schemas (follow in design/API specs).

---

## 2. Product map

| Product | Domain | Role |
| --- | --- | --- |
| **what2eat** | what2eat.food | Thin web + BFF: restaurant recommend UX; place/map via places-agent |
| **where2play** | where2play.place | Thin web + BFF (future): trip UX; itinerary **engine** via places-agent |
| **places-agent** | places.agent-mate.ai | Place tools, vendor adapters, agent LLM, HTTP + MCP, `plan_itinerary`, **and** the operator management web (home, login, admins, caller keys, instructions) |

Consumer apps share a **places.family** footer row (cross-links + copyright). Mock contract: [`../2.what2eat/2eat-specs/2eat-design.md`](../2.what2eat/2eat-specs/2eat-design.md) §1; where2play home mock: [`../3.where2play/2play-specs/ui-mockup/01-home.html`](../3.where2play/2play-specs/ui-mockup/01-home.html).

**Ownership rule:** consumer screens and product Quanzil stay on what2eat / where2play (ADR-001). places-agent owns place/map adapters, the tool loop, itinerary planning, **and operator UX on this host** (ADR-012). The admin app is **not** a fourth product.

```text
Consumer browsers                         Operator browser
what2eat.food / where2play.place          places.agent-mate.ai
(thin web + same-origin BFF)              (admin HTML + same-origin BFF)
         │                                         │
         │ caller API key                          │ admin session
         └──────────────────┬──────────────────────┘
                            ▼
              ┌─────────────────────────────┐
              │ places-agent (one process)  │
              │ places.agent-mate.ai        │
              │ admin UI + HTTP API + MCP   │
              └──────────────┬──────────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
           AMAP        Google Maps    Tripadvisor
```

---

## 3. Runtime trust boundaries

Four runtimes: **consumer browser**, **operator browser**, **app BFF**, **places-agent**. Secrets never ship to either browser. Operator HTML is served from `places.agent-mate.ai` and calls **same-origin** admin APIs only.

### Who may call what

| Capability | Consumer browser | Operator browser (`places.agent-mate.ai`) | App BFF | places-agent |
| --- | --- | --- | --- | --- |
| Quanzil (`OPENAI_*`) | No | No | Yes (product UX) | Yes (agent / tools) |
| AMAP / Google / Tripadvisor keys | No | No (not in admin UI) | No | Yes (env) |
| Admin session (cookie) | No | Yes (same-origin) | No | Issues / validates |
| Caller API key (plaintext) | No | Once at create / regenerate, then copy; never stored in the client as the source of truth | Sends on server-to-server calls | Stores hash; validates HTTP/MCP |
| places-agent HTTP tools | Same-origin **app** routes only | No (session ≠ caller key) | Server-to-server + caller key | Serves |
| places-agent MCP | N/A | N/A | Optional | Serves (e.g. chatboxai) + caller key |
| Resend (invite / reset) | No | Triggers via same-origin admin API | No | Yes (env) |
| Nav deep links / logos | Yes (display / open) | N/A | Pass-through | Emits on cards |

### Request flows

```text
# Operator admin (ADR-012)
Operator browser → places.agent-mate.ai (same origin) → session → users / caller keys / instructions
                 → Resend (invite / password reset)

# Place / map work
Consumer browser → app same-origin API → places-agent (caller API key) → AMAP | Google | Tripadvisor

# Product LLM (app-owned)
Consumer browser → app same-origin API → Quanzil (app OPENAI_*)

# Agent LLM
Caller → app BFF or MCP host → places-agent (caller API key) → Quanzil (agent OPENAI_*)
```

---

## 4. places-agent core

- **Tools (MVP+):** `search_restaurants`, `search_places`, `get_place_details`, `navigate`; geocode as needed; `plan_itinerary` for trip callers.
- **Dual transport:** HTTP API and MCP share one tool core (no forked business logic). Both require a **caller API key**.
- **Adapters:** AMAP, Google Maps, Tripadvisor behind a common place-card shape (`provider` / `sources[]`, deep links).
- **Google transport (ADR-017):** `GOOGLE_MAPS` means Google data. Call **direct** `maps.googleapis.com` first (`GOOGLE_MAPS_API_KEY`). On egress failure only, call the **Cloudflare Worker MCP** (`GMAPS_MCP_URL` + `GMAPS_MCP_BEARER`; `tools/list` first). `sources[]` still says `GOOGLE_MAPS` — never `GMAPS_MCP`. Do not swap in AMAP. This Worker is **not** places-agent’s `/mcp` (that is Feature 11).
- **Weather:** Open-Meteo only (ADR-014). Not a `providers[]` vendor. Localize WMO codes; do not pass English Open-Meteo labels through.
- **Operator web (same process):** public home, login, post-login landing, admin users, caller API keys, agent instructions, admin i18n. Auth = **admin session**. Does not display map-vendor keys. Public register is off; default admin is seeded on first deploy.
- **One hostname:** `places.agent-mate.ai` serves operator HTML, admin APIs, tool HTTP, and MCP. Path layout is an implementation detail; admin APIs stay same-origin with the admin pages.
- **Caller identity:** machine id **`places-agent`** (ADR-013). MCP `serverInfo.name` and HTTP JSON field `agent` MUST be this string. Hostname `places.agent-mate.ai` is the host, not the id. Tool names stay unprefixed (`search_restaurants`, …).
- **Data:** admin users and caller-key hashes persist in a store bound to this stack (volume or DB). Map-vendor keys, Quanzil, Resend, and session secret stay in env.

---

## 5. LLM posture

- All product LLM usage uses **OpenAI Quanzil** (`OPENAI_*`) on the **server** of that deployable.
- LLM is **not** destination-routed (no “CN → Qwen, else Gemini”).
- Optional other Quanzil models (e.g. Gemini via Quanzil) may exist in key inventories for experiments; they are not part of destination routing.

---

## 6. Provider gateway (caller-driven)

**Supersedes** embedded “mainland destination ⇒ AMAP only” geo policy and the former `agent-config/geo-capability-route.json` region matrix as a hard router. That file is **removed**; do not recreate it.

| Rule | Detail |
| --- | --- |
| Selection | Caller passes `providers[]` (and related options: enrich, merge) |
| Agent role | Multi-provider gateway: validate, call adapters, tag results |
| Google transport | Direct `maps.googleapis.com` first; Cloudflare Worker MCP (`GMAPS_MCP_*`) on egress failure only. Provenance `GOOGLE_MAPS` (ADR-017). Not a fourth vendor. Not AMAP-when-Google-fails. Not places-agent `/mcp`. |
| Failures | Missing credentials / unsupported capability → explicit error or skip with reason |
| Config | Secrets only in env; capability matrix in docs/config for *what each vendor can do*, not “where to force” |

Example (conceptual):

```json
{
  "query": "ramen",
  "near": { "lat": 22.3, "lng": 114.2 },
  "providers": ["GOOGLE_MAPS"],
  "enrich": { "tripadvisor": true },
  "merge": true
}
```

Mainland what2eat may request `["AMAP"]`; HK/overseas may request `["GOOGLE_MAPS"]` + Tripadvisor enrich — **caller policy**, not agent-hardcoded destination buckets.

---

## 7. Result model & client navigation

- Cards carry `provider` and/or `sources[]` (id, logo URL, deep links).
- Optional **merge** → one cluster, multiple sources, `primary_provider`.
- **Nav choice is client-side:** e.g. mainland browser prefers AMAP deeplink when present; Google Maps may fail in CN. Agent returns all available links; it does not rewrite UI choice from destination alone.

---

## 8. Tripadvisor enrichment

- Match Tripadvisor by **name + location** (and similar soft signals).
- **Never** pass Google `place_id` (or other Google-native ids) to Tripadvisor as a place identifier.
- Enrichment is optional and best-effort; failures must not wipe primary search results.

---

## 9. Itinerary ownership

| Layer | Owner |
| --- | --- |
| `plan_itinerary` tool / engine | places-agent |
| Trip screens, editing, save/share UX | where2play |

where2play remains thin: collect bounds → call agent tools → present plan.

---

## 10. Deployment (野草云3)

**Host:** 野草云3 / `svr_hk_vps_3` (`38.55.192.140`), same ops pattern as release-bot (GHCR → Portainer → Nginx Proxy Manager → Cloudflare). Apex domains follow the mypoke pattern. kb-agent’s NPM Custom Locations apply only when **two containers** share one hostname; places-agent is **one container**, so that pattern is not copied.

**Operational plan** for release-bot (ports, compose skeletons, env names, smoke): [`6.deployment-plan.md`](./6.deployment-plan.md) **§0 Port & URL registry**.

**Local dev ports (monorepo):** places-agent **`3010`** · what2eat **`3020`** · where2play **`3030`**. Each app’s `PUBLIC_BASE_URL` uses **its own** port. Thin apps call the agent at **`PLACES_AGENT_BASE_URL`** (`http://localhost:3010` locally) — do not point what2eat’s public URL at `:3010`.

**Production (野草云3):** shared node IP `38.55.192.140`; unique host debug binds what2eat **`3004→3000`**, where2play **`3005→3000`**, places-agent **`3007→3000`**; NPM always forwards to **container `:3000`**.

Admin UI does **not** add a fourth stack (ADR-012). It is packaged in the **places-agent** image.

| Principle | Choice |
| --- | --- |
| Services | **3** independent services (unchanged) |
| Images | **3** images, **one process per image** |
| Portainer | Prefer **3** stacks (isolation / blast radius) |
| places-agent image | Operator web + admin BFF + HTTP API + MCP in that **one** process |
| Avoid | Fourth `places-admin` stack; admin-nginx + agent-API as two processes in one container; map-vendor keys in the image or in the admin UI |

```text
GitHub Actions → GHCR
       ↓
Portainer stack: what2eat          → NPM → what2eat.food
Portainer stack: where2play        → NPM → where2play.place
Portainer stack: places-agent      → NPM → places.agent-mate.ai
       (admin HTML + session APIs + HTTP tools + MCP)
       ↓
Cloudflare
```

**places-agent stack (env and data)**

| Kind | Examples | Where |
| --- | --- | --- |
| Map vendor | AMAP / Google / Tripadvisor | Portainer env — never in admin UI or client bundles |
| Agent LLM | `OPENAI_*` (Quanzil) | Portainer env |
| Mail | Resend API key | Portainer env |
| Admin session | Cookie signing secret | Portainer env |
| Durable store | Admin users, hashed caller API keys | Volume or DB attached to this stack |
| Seed | Default admin `admin` / `me@ethanhuang.com` | First boot; public register remains disabled |

NPM terminates TLS for `places.agent-mate.ai` and proxies **all** paths on that hostname to the one container (`/`, `/v1`, `/mcp`, `/sse`, `/messages`). Do not add kb-style Custom Locations to a second upstream. MCP clients use this same host plus a caller API key issued in the admin app.

---

## 11. Workspace & remotes

Parent directory on disk: `~/code/places-workspace/`.

| Path | Remote | Contents |
| --- | --- | --- |
| `places-workspace/` (parent) | https://github.com/ethanhuangcst/places.workspace.git | Umbrella **specs** only (`workspace-specs/`) |
| `0.1.sdd.sample/` | https://github.com/ethanhuangcst/sdd.sample.git | SDD sample / API·SDK·doc patterns |
| `0.2.release-bot/` | https://github.com/ethanhuangcst/release-bot.git | Deploy / Portainer automation |
| `1.places-agent/` | https://github.com/ethanhuangcst/places.agent-mate.ai.git | Agent service (`places.agent-mate.ai`) |
| `2.what2eat/` | https://github.com/ethanhuangcst/what2eat.food.git | what2eat.food web app |
| `3.where2play/` | https://github.com/ethanhuangcst/where2play.places.git | where2play.place web app |

Parent `.gitignore` ignores child clone directories so only umbrella specs are tracked in the parent repo.

---

## 12. ADR index

| ID | Title | One-liner |
| --- | --- | --- |
| ADR-001 | Thin-app / agent split | Consumer web apps stay UX-thin; places-agent owns place tools + itinerary engine |
| ADR-002 | Same-origin BFF trust | Secrets never in browser; map keys only on agent; apps may hold Quanzil on BFF |
| ADR-003 | Dual transport | HTTP + MCP over one tool core |
| ADR-004 | Quanzil fixed per deployable | LLM not destination-routed |
| ADR-005 | Caller-driven providers | `providers[]`; no embedded mainland→AMAP geo force |
| ADR-006 | Provenance + client nav | Tag sources; UI picks deeplink by client environment |
| ADR-007 | Tripadvisor match | Name/location only; no Google `place_id` passthrough |
| ADR-008 | Itinerary ownership | Engine in agent; trip UX in where2play |
| ADR-009 | Deploy Option 1 | 3 services / images / stacks on 野草云3 via release-bot |
| ADR-010 | Umbrella workspace | Parent git = specs only; children = separate remotes |
| ADR-011 | Independent HK/TW locales | Separate catalogs; no OpenCC; three output layers |
| ADR-012 | Admin UI on places-agent | Operator web in the same image/stack/host; session ≠ caller key |
| ADR-013 | Caller agent id | Callers identify this service as `places-agent` (MCP `serverInfo.name`, HTTP `agent`) |
| ADR-014 | Open-Meteo weather | Open-Meteo only; WMO/English output localized via catalogs |
| ADR-015 | SQLite + Prisma (superseded by ADR-025) | Admin store was a volume file; do not use for new work |
| ADR-025 | places-agent Postgres | Dedicated Aliyun db `places_agent`; local `:5436` |
| ADR-016 | Custom HTTP server | `server.ts` dispatches MCP; Next handles HTML + `/v1` |
| ADR-017 | GMaps Worker MCP fallback | Direct Google REST first; Cloudflare Worker MCP on egress failure |
| ADR-018 | MVP by capability | Two capability slices; all admin UI in MVP-1; restaurants then places, itinerary, enrich, and chat |
| ADR-019 | HTTP-first user tests | Automate TC-H01–H15 in CI; ChatBox TC-C deferred; HTTP↔MCP via in-memory TC-H12 |
| ADR-020 | HTTP-only chat and enrich | Six tools on HTTP+MCP; `POST /v1/chat` and `enrich.tripadvisor` stay HTTP-only |
| ADR-021 | Live vendor no fixture | `PLACES_VENDOR_MODE=live` must not return fixture cards/ratings/weather; skip if live client missing |
| ADR-022 | Timed itinerary | `detail: timed` on `plan_itinerary`; meals, legs, weather impact |
| ADR-023 | what2eat Postgres | Thin-app Prisma (out of places-agent slice) |
| ADR-024 | Quality gates on TS 7 | Babel ESLint + separate `tsc`; coverage 80%; E2E `.next-e2e` sidecar |
| ADR-025 | places-agent Postgres | Dedicated Aliyun db `places_agent`; local `:5436` |
| ADR-026 | Region-based provider auto-selection | BFF/agent pick providers by geo; Taiwan excludes AMAP |
| ADR-027 | Decide SearchCache hydrate | `GET /api/decide/current` restores reload; no re-search |
| ADR-028 | Decision history on save | `POST /api/saved` writes `DecisionHistory`; Decide save-only |
| ADR-029 | Decide criteria draft hydrate | URL → sessionStorage draft → cache → profile virgin-only |
| ADR-030 | Geocode-first region detection | Prefer geocode/coords over CJK ratio for ADR-026 |
| ADR-031 | Empty AMAP → Google once | Auto-selected AMAP-only empty search retries Google |

Full ADR write-ups: [`adr/`](./adr/).
