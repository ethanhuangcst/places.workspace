# Deployment plan — places family

**Consumer:** release-bot (conversational semi-auto release on 野草云3). Operators fill placeholders and follow `0.2.release-bot/knowledge/03-semi-auto-release.md`. This file is the family plan so Step 0 is confirmation, not a second architecture.

**This file lives in** the umbrella repo (`workspace-specs/`). Parent git tracks specs only. Child app repos copy or link stack-specific slices into their own `deployment-plan.md` when Docker/CI exist.

**Spec sources:** [`2.architecture.md`](./2.architecture.md) (especially §10), [`3.tech-specs.md`](./3.tech-specs.md), [`knowledge/handbook.md`](./knowledge/handbook.md) §7, [`../1.places-agent/agent-specs/5.agent-design.md`](../1.places-agent/agent-specs/5.agent-design.md) §2 path map. Binding ADRs: ADR-009, ADR-012, ADR-015–025 (do not duplicate their full text).

**Slice (capability, not Docker):** places-agent **MVP-1 and MVP-2 accepted** 2026-08-19 (operator confirmed usable). Tools: six HTTP+MCP + HTTP-only `POST /v1/chat` and `enrich.tripadvisor` (ADR-020). Timed `plan_itinerary` (ADR-022). Live vendors must not serve fixture (ADR-021). Quality gates (ADR-024) are **local** (`make quality`); they do not run in the prod image. **First production stack is still places-agent.** Docker/CI for that stack remain **blockers** until landed in the app repo.

**Secrets:** never in this file, git, Portainer screenshots, or chat. Use Portainer env / node `.env` / local key store. Env **names** only. Do not paste caller API key plaintext, `BOOTSTRAP_ADMIN_PASSWORD` values, or vendor keys here.

**AI posture:** places-agent is an **inference-only** Quanzil tool-loop (ADR-004). It is not a training or MLOps platform. Do **not** add Kubeflow, MLflow, a feature store, a model registry, GPU serving, eval-set CI, or canary model routing. Version inference artifacts from day one via env: `PROMPT_ID`, `GLOSSARY_ID`, `CATALOG_PACK`. Rollback = pin `IMAGE_TAG` **and** those ids.

---

## 0. Meta

| Field | Value |
| --- | --- |
| Product family | places (what2eat, where2play, places-agent) |
| Target node | **野草云3** / `svr_hk_vps_3` · public IP **`38.55.192.140`** (inventory: `0.2.release-bot/svr_hk_vps_3/hk_vps_3_setting.md`, as_of 2026-08-12) |
| Pipeline | GitHub Actions → GHCR → Portainer **pull-only** → Nginx Proxy Manager → Cloudflare |
| Shared network | `portainer_network` (external; **never delete or recreate**) |
| Portainer | `https://portainer.agent-mate.ai` |
| NPM | `https://nginx.agent-mate.ai` |
| Production path | Portainer + GHCR + NPM on 野草云3 — **not** Caddy/PM2 on bare metal; **not** `docker build` on the VPS |
| Deployables | **Three** (ADR-009). **Not four.** |
| First production stack | **places-agent** (ADR-018). Capability slice MVP-1+MVP-2 **accepted** locally 2026-08-19; GHCR/Portainer still gated on Docker/CI blockers below. |
| Default git ref | `main` (or release tag `v*`). `IMAGE_TAG` is a **published GHCR tag**, never git branch `main` (release-bot ADR-002) |

### Port & URL registry (canonical — do not duplicate)

**One node, many apps:** all three stacks share public IP **`38.55.192.140`**. That is intentional. **Collisions to avoid** are **host debug bind ports** (野草云3) and **local dev `PORT` / `PUBLIC_BASE_URL` ports** on the developer machine.

| Layer | Rule |
| --- | --- |
| Public DNS | Each product has its **own apex/subdomain** — never reuse another app’s hostname in `PUBLIC_BASE_URL` |
| NPM upstream | Always **container name + container port `3000`** — never another stack’s host debug port |
| Host debug bind | **One unique host port per stack** on 野草云3 (debug / curl only; not NPM Forward Port) |
| Local dev | **One unique `PORT` per app** in the monorepo; `PUBLIC_BASE_URL` must use **that app’s** port |
| BFF → agent | Thin apps call **places-agent’s** URL/port — not their own `PORT` |

#### 野草云3 — host debug binds (confirm with `ss -lntup` before first deploy)

Inventory as_of 2026-08-12 — do not take `3001`–`3003`, `3006`, `3200`–`3203`, `6333`, `6335`, `6336` (see release-bot `svr_hk_vps_3/hk_vps_3_setting.md` §5).

| Stack | Host bind (debug) | Container | Container listen | NPM Forward Hostname | NPM Forward Port | Public URL |
| --- | --- | --- | --- | --- | --- | --- |
| `what2eat` | **`3004→3000`** | `what2eat-web` | `3000` | `what2eat-web` | **`3000`** | `https://what2eat.food` |
| `where2play` | **`3005→3000`** | `where2play-web` | `3000` | `where2play-web` | **`3000`** | `https://where2play.place` |
| `places-agent` | **`3007→3000`** | `places-agent` | `3000` | `places-agent` | **`3000`** | `https://places.agent-mate.ai` |

NPM must **not** forward to host `3004`/`3005`/`3007` — only to **container port `3000`** on `portainer_network`.

#### Local dev — monorepo ports (`.env.local`)

| App | Folder | `PORT` | Postgres (local) | `PUBLIC_BASE_URL` / `APP_URL` | Upstream to places-agent |
| --- | --- | --- | --- | --- | --- |
| places-agent | `1.places-agent/` | **`3010`** | **`5435`** (shared instance, db `places_agent`) or **`5436`** (optional compose) | `http://localhost:3010` | — (this **is** the agent) |
| what2eat | `2.what2eat/` | **`3020`** | **`5435`** | `http://localhost:3020` | `PLACES_AGENT_BASE_URL=http://localhost:3010` |
| where2play | `3.where2play/` | **`3030`** | TBD | `http://localhost:3030` | `PLACES_AGENT_BASE_URL=http://localhost:3010` |

**Anti-patterns (duplicate / wrong app):**

- what2eat `PORT=3010` or `PUBLIC_BASE_URL=http://localhost:3010` — collides with places-agent’s own origin
- what2eat `PLACES_AGENT_BASE_URL=http://localhost:3020` — points at itself, not the agent
- Any stack reusing another stack’s **host debug** port on 野草云3
- Putting `38.55.192.140:3007` (or any host bind) in NPM Forward Port — use container `:3000`

**Cross-machine LAN dev** (phone / second laptop): set each app’s `PUBLIC_BASE_URL` to `http://<host-lan-ip>:<that-app-PORT>` and list the LAN IP in `ALLOWED_DEV_ORIGINS`. Example when host is `192.168.1.10`:

| App | `PUBLIC_BASE_URL` | `ALLOWED_DEV_ORIGINS` |
| --- | --- | --- |
| places-agent | `http://192.168.1.10:3010` | `192.168.1.10` |
| what2eat | `http://192.168.1.10:3020` | `192.168.1.10` |
| what2eat → agent | `PLACES_AGENT_BASE_URL=http://192.168.1.10:3010` | — |

#### Production — thin app → agent URL

| Caller | `PLACES_AGENT_BASE_URL` (preferred names) | When |
| --- | --- | --- |
| what2eat / where2play on 野草云3 | `http://places-agent:3000` | Both stacks on `portainer_network`; lowest latency |
| what2eat / where2play (smoke / fallback) | `https://places.agent-mate.ai` | Public path; same as external clients |
| Local what2eat | `http://localhost:3010` | Agent running via `1.places-agent` `make dev` |

#### Local dev — what2eat Postgres ([ADR-023](./adr/ADR-023-what2eat-postgres-prisma.md))

| Item | Value |
| --- | --- |
| Compose | `2.what2eat/docker-compose.dev.yml` |
| Container | `what2eat-postgres` |
| Host bind | **`5435→5432`** (avoid local `5432` / other stacks’ `5434`) |
| Default URL | `postgresql://what2eat:what2eat@localhost:5435/what2eat` |
| Prod | External Postgres **`101.132.156.250:5432/what2eat`** — dedicated DB; not on 野草云3 |

Env name standard: **`PLACES_AGENT_BASE_URL`** + **`PLACES_AGENT_CALLER_KEY`** (what2eat). Legacy alias: `PLACES_AGENT_URL` / `PLACES_AGENT_API_KEY` — do not mix both in one stack.

Child detail: [`../2.what2eat/2eat-specs/6.deployment-plan.md`](../2.what2eat/2eat-specs/6.deployment-plan.md).

### Per-stack table

| Stack | App repo | Local folder | App slug | Public domain | `PUBLIC_BASE_URL` | Process | First-wave? |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `places-agent` | `ethanhuangcst/places.agent-mate.ai` | `1.places-agent/` | `places-agent` | **`places.agent-mate.ai`** | `https://places.agent-mate.ai` | **ONE** Node process: operator HTML + admin BFF + `/v1` HTTP tools + MCP. Entry `server.ts`. Local **`PORT=3010`** · prod host debug **`3007→3000`**. | **Yes — first** |
| `what2eat` | `ethanhuangcst/what2eat.food` | `2.what2eat/` | `what2eat` | **`what2eat.food`** (apex) | `https://what2eat.food` | Thin Next web + BFF → places-agent. **PostgreSQL** (profile/saved/history; [ADR-023](./adr/ADR-023-what2eat-postgres-prisma.md)). Chat in browser localStorage. **No** map keys; **no** product `OPENAI_*`. Local **`PORT=3020`** · Postgres **`5435`** · prod host debug **`3004→3000`**. | Second, after agent can `search_restaurants` |
| `where2play` | `ethanhuangcst/where2play.places` | `3.where2play/` | `where2play` | **`where2play.place`** (apex) | `https://where2play.place` | Thin-app pattern. Agent tools it needs (`search_places`, `plan_itinerary`) exist on places-agent. Local **`PORT=3030`** · prod host debug **`3005→3000`**. | Third |

**Forbidden:** fourth `places-admin` stack; MCP sidecar; two processes in one image; admin-nginx + agent-API; recreating `portainer_network`; sharing Aliyun DB `what2eat` / `kb_agent` / `mypoke_trade_prod` with places-agent (ADR-025 — dedicated `places_agent`).

**Unlike kb-agent:** kb-agent splits `kb-web` and `kb-agent` so NPM Custom Locations route `/mcp` to a **second container**. places-agent does **not**. One hostname → **one container**. NPM still needs **SSE / long timeouts** on that host. After stack recreate, still **Save** the NPM host (release-bot ADR-003 applies even without Custom Locations).

Google Maps Cloudflare Worker MCP (`GMAPS_MCP_*`) is **not** a Portainer stack on 野草云3. It is a separate Cloudflare Worker. Provenance stays `GOOGLE_MAPS` (ADR-017).

Host / local port tables: **§0 Port & URL registry** (canonical). Do not duplicate port numbers across stacks or reuse places-agent’s local port for what2eat’s `PUBLIC_BASE_URL`.

---

## Blockers / app-repo readiness

release-bot **must not** start Portainer (Steps 3–5) for a stack until that stack’s Docker/CI artifacts exist in **its app repo**. Missing items below are **blockers**, not optional polish.

### places-agent (`1.places-agent/` / `ethanhuangcst/places.agent-mate.ai`)

**Has (verified, 2026-08-19):**

- Next standalone (`output: "standalone"`)
- Custom entry `server.ts` (ADR-016); `package.json` `start` = `node server.ts`
- Prisma (PostgreSQL, ADR-025) + `prisma/seed.ts` (reads `DEV_ADMIN_PASSWORD` locally; prod uses `BOOTSTRAP_ADMIN_PASSWORD` when wired)
- Makefile (`dev` / `up` / `down` / `quality` / `test-coverage` / `test-e2e`)
- `.env.example` (local) and **`.env.production.example`** (Portainer names)
- MVP-2 tools in code: `search_places`, `plan_itinerary` (`detail: timed`), Tripadvisor enrich, `POST /v1/chat`

**Does not yet have (blockers for release-bot Steps 1–2, then Portainer):**

| Artifact | Status |
| --- | --- |
| `Dockerfile` (`CMD` = `node server.ts`; migrate + seed on boot) | **Missing — blocker** |
| `docker-compose.prod.yml` (image-only, external `portainer_network`) | **Missing — blocker** |
| `.github/workflows/ghcr.yml` | **Missing — blocker** |
| Seed reads `BOOTSTRAP_ADMIN_PASSWORD` in prod | **Confirm in repo** — `prisma/seed.ts` historically used `DEV_ADMIN_PASSWORD` only. Until seed is updated, prod first boot is empty hash → `/set-password`. **Never** set `DEV_ADMIN_PASSWORD` in Portainer. |
| Child-repo `deployment-plan.md` | Later; this umbrella file is the family source until then |

### what2eat / where2play

**Current truth:** specs folders + logos only; **no app runtime**, no `package.json`, no Docker/CI.

| Artifact | Status |
| --- | --- |
| Next app / BFF | **Missing — blocker** (do not wait for these before writing this family plan; do wait before deploying those stacks) |
| `Dockerfile` / `docker-compose.prod.yml` / `.github/workflows/ghcr.yml` / `.env.prod.example` | **Later blockers** |

Do **not** wait for all three Dockerfiles before using this family plan for **places-agent** once that repo’s blockers clear.

---

## 1. Architecture (runtime)

```text
[Consumer browsers]                    [Operator browser]              [MCP clients]
what2eat.food / where2play.place       places.agent-mate.ai            Cursor / ChatBox
(thin web + same-origin BFF)           (admin HTML + same-origin BFF)  Bearer caller key
         │                                      │                              │
         │ PLACES_AGENT_CALLER_KEY (server)         │ admin session cookie         │
         └──────────────────────────────────────┼──────────────────────────────┘
                                                ▼
                         Cloudflare DNS (grey cloud until Let's Encrypt OK)
                                                ▼
                         Nginx Proxy Manager on 野草云3 (:80 / :443)
                                                ▼
              ┌─────────────────────────────────┼─────────────────────────────────┐
              ▼                                 ▼                                 ▼
     what2eat-web:3000                 places-agent:3000                  where2play-web:3000
     (stack what2eat)                  (ONE process, ADR-016)             (stack where2play)
                                       server.ts → MCP + Next
                                                │
                         ┌──────────────────────┼──────────────────────┐
                         ▼                      ▼                      ▼
                      AMAP              Google Maps REST         Tripadvisor Terra
                                        └→ Cloudflare Worker MCP
                                           (not on this VPS)
                         Quanzil (OPENAI_* on this stack)     Open-Meteo     Resend
                         Postgres `places_agent` on Aliyun (ADR-025)
```

### Process model

| Deployable | Containers in stack | In-process | Independent container? |
| --- | --- | --- | --- |
| places-agent | **1** (`places-agent`) | Operator HTML, `/api/admin/*`, `/v1/*`, `/mcp`, `/sse`, `/messages` | No sidecar. No second Node. Local **`PORT=3010`** · prod host debug **`3007→3000`**. |
| what2eat | **1** (`what2eat-web`) | Consumer HTML + same-origin BFF | No agent process in this image |
| where2play | **1** (`where2play-web`) | Same thin-app pattern | No agent process in this image |

**Not on 野草云3 as a stack:** Google Maps Cloudflare Worker MCP. Configure `GMAPS_MCP_*` on the places-agent env only.

**Not in this family:** Qdrant, RAG sidecar, Aliyun Postgres for places-agent, Kubeflow/MLflow/GPU.

### Public vs private

| Surface | Public via NPM? | Auth |
| --- | --- | --- |
| Operator HTML on `places.agent-mate.ai` | Yes | Session cookie (admin). Public register **off**. |
| Admin BFF `/api/admin/*` | Yes (same origin) | Session + CSRF |
| HTTP tools `/v1/*` | Yes | Bearer caller API key (except health) |
| Health `/v1/health` and `/health` | Yes | None. Body `{ "agent": "places-agent", "ok": true }` |
| MCP `/mcp` (Cursor) | Yes | Bearer caller API key |
| MCP `/sse` + `/messages` (ChatBox) | Yes | Bearer caller API key |
| Host-port binds (`3004`/`3005`/`3007`) | Debug only; do not publish as the public path | — |
| SQLite file `/data/places-agent.db` | **No** | Removed (ADR-025) |
| Map keys, Quanzil, Resend, MCP bearer, caller-key plaintext | **Never** in browsers or this plan | Portainer env |

Caller-visible agent id is **`places-agent`** (ADR-013) — MCP `serverInfo.name` and HTTP JSON `agent`. Hostname `places.agent-mate.ai` is the host, not the id.

### Persistence

| Data | Where | Volume |
| --- | --- | --- |
| Admin users, hashed caller API keys | **PostgreSQL** + Prisma ([ADR-025](./adr/ADR-025-places-agent-postgres-prisma.md)) | External **`101.132.156.250:5432/places_agent`** (prod); local **`localhost:5436`** |
| what2eat users, tastes, saved, history | **PostgreSQL** + Prisma ([ADR-023](./adr/ADR-023-what2eat-postgres-prisma.md)) | External **`101.132.156.250:5432/what2eat`** (prod); local **`localhost:5435`** |
| where2play | TBD per app spec | No volume unless uploads |

Migrate + seed on boot (ADR-025). Tests never use production Aliyun (`TEST_DATABASE_URL` locally → `places_agent_test`).

### Outbound vendors (places-agent only)

| Vendor | Env family | Notes |
| --- | --- | --- |
| Quanzil | `OPENAI_*` | `openai` SDK; base URL `https://quanzil.com/v1`; **never** `api.openai.com`; `max_completion_tokens`. Per-deployable, not destination-routed (ADR-004). |
| AMAP Web 服务 | `AMAP_*` | Server-side only |
| Google Maps Platform (direct) | `GOOGLE_MAPS_*` | First path for `GOOGLE_MAPS` |
| Google Maps Worker MCP | `GMAPS_MCP_*` | Fallback on egress failure only (ADR-017). Worker is Cloudflare, not Portainer. |
| Tripadvisor Terra | `TRIPADVISOR_*` | HTTP enrich (`enrich.tripadvisor`); not an MCP tool (ADR-020) |
| Open-Meteo | `OPEN_METEO_*` | Timed itinerary weather; not a `providers[]` vendor |
| Resend | `RESEND_*` | Admin invite / password reset |

what2eat / where2play outbound: HTTPS to **`PLACES_AGENT_BASE_URL`** with **`PLACES_AGENT_CALLER_KEY`**. **No** map vendor keys. what2eat chat uses agent `POST /v1/chat` — **no** product `OPENAI_*` on what2eat ([`2.what2eat/2eat-specs/5.tech-design.md`](../2.what2eat/2eat-specs/5.tech-design.md)).

### LLM / AI ops (keep boring)

| Topic | Choice |
| --- | --- |
| Serving | Real-time HTTP/MCP inference via Quanzil. CPU Node process. |
| Training / registry / GPU | **Out of scope.** Do not introduce. |
| Prompt versioning | Env `PROMPT_ID`, `GLOSSARY_ID`, `CATALOG_PACK` from day one |
| Rollback | Pin `IMAGE_TAG` + those three ids. Do not “roll back the model” via a registry. |
| Observability (now) | `/v1/health`; vendor/adapter error logs; latency. Defer eval sets, quotas, canary models. |
| Chat | `POST /v1/chat` on places-agent (HTTP-only, ADR-020). Requires `OPENAI_*` in production. **Do not** set `QUANZIL_MODE=fixture` in Portainer. |
| LLM location | Agent Quanzil on places-agent only. what2eat has **no** `OPENAI_*`; chat goes through the agent. |

---

## 2. Services table

| Service | Stack | container_name | Image (proposed GHCR path until CI exists) | Container port | Host port | Public? | Role |
| --- | --- | --- | --- | --- | --- | --- | --- |
| agent | `places-agent` | `places-agent` | `ghcr.io/ethanhuangcst/places.agent-mate.ai/agent:<tag>` | `3000` | **proposed `3007`** | yes via NPM | Operator UI + admin BFF + `/v1` + MCP |
| web | `what2eat` | `what2eat-web` | `ghcr.io/ethanhuangcst/what2eat.food/web:<tag>` | `3000` | **proposed `3004`** | yes via NPM | Thin consumer web + BFF |
| web | `where2play` | `where2play-web` | `ghcr.io/ethanhuangcst/where2play.places/web:<tag>` | `3000` | **proposed `3005`** | yes via NPM | Thin consumer web + BFF |

GHCR image **names** are proposed until each repo’s Dockerfile/workflow lands. Tags must be published (`sha` / `v*` / `latest`), never branch `main`.

---

## 3. Images & CI

Portainer **only pulls**. Never build app images on 野草云3. GitHub Actions builds on GitHub-hosted runners. HK npm Aliyun mirror `https://registry.npmmirror.com` applies to **builds on 野草云3 if any** (ops tools); it is **not** the prod image build path.

| Item | places-agent | what2eat | where2play |
| --- | --- | --- | --- |
| Dockerfile | **Blocker** — land in app repo; `CMD` `node server.ts`; Next standalone copy; Prisma migrate + seed then `exec node server.ts` | Later blocker | Later blocker |
| Workflow | **Blocker** `.github/workflows/ghcr.yml` | Later | Later |
| Registry | `ghcr.io` | `ghcr.io` | `ghcr.io` |
| Proposed image | `…/places.agent-mate.ai/agent` | `…/what2eat.food/web` | `…/where2play.places/web` |
| Tags | `latest` (default branch) + git sha + `v*` | same when CI exists | same when CI exists |
| Node / Next | Next **16.3** standalone; keep patched (ClickFix lesson) | Same family stack when the app exists | Same |
| Playwright in prod image | No | No unless an app spec requires it | No |

If GHCR packages are private, Portainer needs a PAT with `read:packages` (ops key store — not in git).

Portainer often does **not** re-pull `latest` on Update — require **Recreate + Pull**, or pin sha.

---

## 4. Compose contract

Skeletons below are **to land in child repos**, not in the umbrella git. Until they exist, treat § Blockers as closed gates.

| Item | places-agent | what2eat / where2play |
| --- | --- | --- |
| Path (when landed) | `docker-compose.prod.yml` at app-repo root | Same |
| App services | `image:` only — **no** `build:` | Same |
| Network | `networks.default.external: true` + `name: portainer_network` | Same |
| Volumes | **None** (Postgres off-node, ADR-025) | **None** (Postgres off-node); where2play none unless uploads |
| Special flags | N/A (no Playwright/Xvfb) | N/A |

### Proposed `docker-compose.prod.yml` — places-agent (land in `1.places-agent/`)

Host port **`3007` is proposed**. Confirm on node before first deploy.

```yaml
name: places-agent

services:
  agent:
    image: ghcr.io/ethanhuangcst/places.agent-mate.ai/agent:${IMAGE_TAG:-latest}
    container_name: places-agent
    restart: unless-stopped
    ports:
      - "3007:3000"
    environment:
      NODE_ENV: production
      APP_NAME: places-agent
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      DATABASE_URL: ${DATABASE_URL:?set DATABASE_URL}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-https://places.agent-mate.ai}
      APP_URL: ${APP_URL:-https://places.agent-mate.ai}
      SESSION_SECRET: ${SESSION_SECRET:?set SESSION_SECRET}
      PROMPT_ID: ${PROMPT_ID:-chat.v1}
      GLOSSARY_ID: ${GLOSSARY_ID:-}
      CATALOG_PACK: ${CATALOG_PACK:-catalogs.v1}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_HOST: ${OPENAI_HOST:-quanzil.com}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://quanzil.com/v1}
      OPENAI_CHAT_MODEL: ${OPENAI_CHAT_MODEL:-}
      AMAP_API_KEY: ${AMAP_API_KEY:-}
      AMAP_HOST: ${AMAP_HOST:-restapi.amap.com}
      AMAP_BASE_URL: ${AMAP_BASE_URL:-https://restapi.amap.com}
      GOOGLE_MAPS_API_KEY: ${GOOGLE_MAPS_API_KEY:-}
      GOOGLE_MAPS_HOST: ${GOOGLE_MAPS_HOST:-maps.googleapis.com}
      GOOGLE_MAPS_BASE_URL: ${GOOGLE_MAPS_BASE_URL:-}
      GOOGLE_MAPS_PROJECT: ${GOOGLE_MAPS_PROJECT:-}
      GMAPS_MCP_HOST: ${GMAPS_MCP_HOST:-}
      GMAPS_MCP_URL: ${GMAPS_MCP_URL:-}
      GMAPS_MCP_BEARER: ${GMAPS_MCP_BEARER:-}
      TRIPADVISOR_API_KEY: ${TRIPADVISOR_API_KEY:-}
      TRIPADVISOR_HOST: ${TRIPADVISOR_HOST:-terra.tripadvisor.com}
      TRIPADVISOR_BASE_URL: ${TRIPADVISOR_BASE_URL:-https://terra.tripadvisor.com/api}
      OPEN_METEO_HOST: ${OPEN_METEO_HOST:-api.open-meteo.com}
      OPEN_METEO_BASE_URL: ${OPEN_METEO_BASE_URL:-https://api.open-meteo.com/v1}
      OPEN_METEO_API_KEY: ${OPEN_METEO_API_KEY:-}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_HOST: ${RESEND_HOST:-api.resend.com}
      RESEND_BASE_URL: ${RESEND_BASE_URL:-https://api.resend.com}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL:-}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:-}
    networks:
      - default

networks:
  default:
    external: true
    name: portainer_network
```

Do **not** set `DEV_ADMIN_PASSWORD` in production compose. After first admin password is set, **unset** `BOOTSTRAP_ADMIN_PASSWORD` from the Portainer stack env.

### Proposed `docker-compose.prod.yml` — what2eat (land in `2.what2eat/` later)

Host port **`3004`** per §0 registry. Full slice: [`../2.what2eat/2eat-specs/6.deployment-plan.md`](../2.what2eat/2eat-specs/6.deployment-plan.md) §4.

```yaml
name: what2eat

services:
  web:
    image: ghcr.io/ethanhuangcst/what2eat.food/web:${IMAGE_TAG:-latest}
    container_name: what2eat-web
    restart: unless-stopped
    ports:
      - "3004:3000"
    environment:
      NODE_ENV: production
      APP_NAME: what2eat
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      DATABASE_URL: ${DATABASE_URL:?set DATABASE_URL}
      SESSION_SECRET: ${SESSION_SECRET:?set SESSION_SECRET}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-https://what2eat.food}
      APP_URL: ${APP_URL:-https://what2eat.food}
      PLACES_AGENT_BASE_URL: ${PLACES_AGENT_BASE_URL:-http://places-agent:3000}
      PLACES_AGENT_CALLER_KEY: ${PLACES_AGENT_CALLER_KEY:?set PLACES_AGENT_CALLER_KEY}
      FEATURE_EMAIL: ${FEATURE_EMAIL:-true}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL:-}
    networks:
      - default

networks:
  default:
    external: true
    name: portainer_network
```

### Proposed `docker-compose.prod.yml` — where2play (land in `3.where2play/` later)

Same shape as what2eat: stack `where2play`, image `ghcr.io/ethanhuangcst/where2play.places/web:${IMAGE_TAG:-latest}`, `container_name: where2play-web`, host **`3005→3000`**, `PUBLIC_BASE_URL=https://where2play.place`, `PLACES_AGENT_BASE_URL` + `PLACES_AGENT_CALLER_KEY`. No map keys. No volume unless a later spec adds uploads.

---

## 5. Environment variables

Names only. Values live in Portainer / node env / local key store. Local: `1.places-agent/.env.example`. Production names: `1.places-agent/.env.production.example`.

### places-agent

| Name | Required | Notes |
| --- | --- | --- |
| `APP_NAME` | yes | `places-agent` (caller-visible id; not the hostname) |
| `PORT` | yes | Container listen `3000` |
| `HOSTNAME` | yes | `0.0.0.0` in Docker |
| `PUBLIC_BASE_URL` / `APP_URL` | yes | `https://places.agent-mate.ai` (invite/reset links, absolute URLs) |
| `SESSION_SECRET` | yes | Admin cookie signing. Rotate only with a planned session wipe. |
| `DATABASE_URL` | yes | Prod `postgresql://…@101.132.156.250:5432/places_agent`. Local `postgresql://places_agent:places_agent@localhost:5435/places_agent` (or `:5436`). Dedicated db — never `what2eat`. |
| `PROMPT_ID` | yes | Inference prompt pack id (default `chat.v1`). Pin on rollback. |
| `GLOSSARY_ID` | no | HK/TW glossary pack; empty until chat/narrative needs it. Pin on rollback. |
| `CATALOG_PACK` | yes | i18n catalog pack (default `catalogs.v1`). Pin on rollback. |
| `OPENAI_API_KEY` | yes | Agent Quanzil for `POST /v1/chat`. **Never** `api.openai.com`. |
| `OPENAI_HOST` | yes | `quanzil.com` |
| `OPENAI_BASE_URL` | yes | `https://quanzil.com/v1` — **never** `api.openai.com` |
| `OPENAI_CHAT_MODEL` | yes | Inventory default `gpt-5.4` |
| `OPENAI_KEY_MGMT_SITE` | no | Console URL; not required at runtime |
| `OPENAI_IMAGE_MODEL` | no | N/A for agent MVP tools |
| `AMAP_API_KEY` | yes for AMAP callers | Web 服务 key only |
| `AMAP_HOST` / `AMAP_BASE_URL` | yes when AMAP used | `restapi.amap.com` / `https://restapi.amap.com` |
| `GOOGLE_MAPS_API_KEY` | yes for Google callers | Direct REST; never `NEXT_PUBLIC_*` |
| `GOOGLE_MAPS_HOST` / `GOOGLE_MAPS_BASE_URL` / `GOOGLE_MAPS_PROJECT` | as needed | Direct Google |
| `GMAPS_MCP_HOST` / `GMAPS_MCP_URL` / `GMAPS_MCP_BEARER` | yes for Google fallback | Worker is not on this VPS. Unconfigured → skip Google after direct failure. |
| `TRIPADVISOR_API_KEY` / `TRIPADVISOR_HOST` / `TRIPADVISOR_BASE_URL` | yes for enrich | Terra host only. HTTP search flag; not MCP. |
| `OPEN_METEO_HOST` / `OPEN_METEO_BASE_URL` / `OPEN_METEO_API_KEY` | yes for timed weather | Free host needs no key; commercial host does |
| `PLACES_VENDOR_MODE` | yes | Production: **`live`**. Never `fixture` in Portainer. |
| `GOOGLE_DIRECT_FORCE_FAIL` | **must not set** | Dev-only. `NODE_ENV=production` rejects at startup. |
| `QUANZIL_MODE` | **must not set** | Dev/E2E fixture LLM only. |
| `RESEND_API_KEY` / `RESEND_HOST` / `RESEND_BASE_URL` / `RESEND_FROM_EMAIL` | yes for invite/reset | Admin mail only |
| `IMAGE_TAG` | yes (Portainer) | Published GHCR tag (sha / `v*` / `latest`), **never** `main` |
| `DEV_ADMIN_PASSWORD` | **local only** | Empty in prod images. Do not set in Portainer. |
| `BOOTSTRAP_ADMIN_PASSWORD` | one-time prod | Proposed name for first-boot hash. Seed username `admin`, email `me@ethanhuang.com`. **Unset after first login / password set.** Never bake a password into the image. If left empty, seed uses empty hash and first login must hit `/set-password` (design path map). |

Public register stays off.

### what2eat / where2play (thin; land with those apps)

Port / URL rules: **§0 Port & URL registry**. Local templates: `2.what2eat/.env.example`, (future) `3.where2play/.env.example`.

| Name | Required | Notes |
| --- | --- | --- |
| `APP_NAME` | yes | `what2eat` or `where2play` |
| `PORT` | yes | **Local dev:** `3020` (what2eat) / `3030` (where2play). **Container prod:** `3000` |
| `HOSTNAME` | yes | `0.0.0.0` in Docker |
| `PUBLIC_BASE_URL` / `APP_URL` | yes | Prod: `https://what2eat.food` or `https://where2play.place`. Local: `http://localhost:<this-app-PORT>` — **never** `:3010` on what2eat |
| `DATABASE_URL` | yes (what2eat) | **PostgreSQL** only. Local `postgresql://what2eat:what2eat@localhost:5435/what2eat`. Prod `postgresql://…@101.132.156.250:5432/what2eat` (Portainer secret) |
| `TEST_DATABASE_URL` | tests | Local `…/what2eat_test`; never prod |
| `SESSION_SECRET` | yes (what2eat MVP-1+) | Consumer session cookie |
| `PLACES_AGENT_BASE_URL` | yes (when BFF exists) | Local: `http://localhost:3010`. Prod on network: `http://places-agent:3000`. Smoke: `https://places.agent-mate.ai` |
| `PLACES_AGENT_CALLER_KEY` | yes (when BFF exists) | Server-only; issued in places-agent admin. **Never** `NEXT_PUBLIC_*` |
| `PLACES_AGENT_TIMEOUT_MS` | no | Default `25000` |
| `RESEND_*` / `FEATURE_EMAIL` | what2eat MVP-1 | Password reset |
| `ALLOWED_DEV_ORIGINS` | local only | LAN IP for cross-machine dev — same IP allowed on each app; **ports differ per app** |
| `IMAGE_TAG` | yes (Portainer) | Published GHCR tag |
| `OPENAI_*` | **no on what2eat** | Chat via agent `/v1/chat`. where2play TBD per spec |
| Map / `GMAPS_MCP_*` / `AMAP_*` / `TRIPADVISOR_*` | **no** | Forbidden on these stacks |
| Legacy `PLACES_AGENT_URL` / `PLACES_AGENT_API_KEY` | deprecated | Use `PLACES_AGENT_BASE_URL` / `PLACES_AGENT_CALLER_KEY` |

---

## 6. Database

| Item | places-agent | what2eat | where2play |
| --- | --- | --- | --- |
| Engine | **PostgreSQL + Prisma** ([ADR-025](./adr/ADR-025-places-agent-postgres-prisma.md)) | **PostgreSQL + Prisma** ([ADR-023](./adr/ADR-023-what2eat-postgres-prisma.md)) | TBD |
| Location | Aliyun Postgres **`places_agent`** | Aliyun Postgres **`what2eat`** | — |
| `DATABASE_URL` | `postgresql://…@101.132.156.250:5432/places_agent` | `postgresql://…@101.132.156.250:5432/what2eat` | — |
| Aliyun Postgres `101.132.156.250` | **Yes — dedicated DB `places_agent`** | **Yes — dedicated DB `what2eat`** | TBD |
| Migrate | On **boot** | On **boot** | TBD |
| Seed | places-agent admin bootstrap | App-specific if needed | TBD |
| Tests | Isolated db `places_agent_test`; never production Aliyun | Isolated db `what2eat_test` | TBD |
| Isolation | Do not write other apps’ DBs | Do not write `places_agent` | — |
| Image rollback | Backup / snapshot **`places_agent`** before schema migrate | Backup / snapshot **`what2eat`** DB before schema migrate | — |

Verify after boot: Prisma can reach `places_agent`; `GET /v1/health` returns ok.

---

## 7. DNS & TLS

Prefer **DNS only (grey cloud)** until Let’s Encrypt succeeds on NPM; orange later if desired. Prefer grey Cloudflare until LE OK (ClickFix / HTML-inject lesson: do not put the hostname on the public Internet until the new container is clean).

| Stack | Zone | Record | Type | Target | Notes |
| --- | --- | --- | --- | --- | --- |
| places-agent | `agent-mate.ai` | `places` | A | `38.55.192.140` | FQDN **`places.agent-mate.ai`**. Exact spelling; typos break SNI. |
| what2eat | `what2eat.food` | apex (`@`) + `www` if used | A (mypoke pattern) | `38.55.192.140` | Apex domain, not a subdomain of `agent-mate.ai`. |
| where2play | `where2play.place` | apex (`@`) + `www` if used | A (mypoke pattern) | `38.55.192.140` | Apex. Domain is `where2play.place` (not `.places`). |

Certificate domain on NPM must match the public hostname **exactly**. Touch **only** this family’s DNS records.

### NPM Proxy Host(s)

**places-agent — one host, one upstream, all paths**

| Field | Value |
| --- | --- |
| Domain | `places.agent-mate.ai` |
| Scheme | `http` |
| Forward hostname | `places-agent` |
| Forward port | `3000` (container port, **not** proposed host `3007`) |
| SSL | Force SSL; certificate for `places.agent-mate.ai` |
| Custom Locations | **Do not** add kb-style path routes to a second container. There is no second container. |

**what2eat / where2play** (when those stacks exist): one host each → `what2eat-web:3000` / `where2play-web:3000`. Force SSL. No Custom Locations.

### MCP client table (places-agent)

Do **not** put plaintext caller keys in this plan, git, or screenshots. Operators paste keys only into the local client.

| Client | Transport | URL | Auth | Do not |
| --- | --- | --- | --- | --- |
| Cursor (and similar Streamable HTTP hosts) | Streamable HTTP | `https://places.agent-mate.ai/mcp` | Header `Authorization: Bearer <caller_api_key>` | Do not use `/sse`. Do not use loopback in product config. |
| ChatBox | Remote SSE (`http/sse`) | `https://places.agent-mate.ai/sse` | Header `Authorization=Bearer <caller_api_key>` (`NAME=VALUE` one line) | Do **not** point ChatBox at `/mcp` (SSE GET → 404). |
| Health check | HTTP GET | `https://places.agent-mate.ai/v1/health` (alias `/health`) | None | Do not treat homepage `200` as MCP healthy. |
| `/messages` | Proxied on the **same** host/container | Handshake-issued; do not hand-edit | Bearer as for `/sse` | Do not route `/messages` to another container. |

Caller-visible agent id: **`places-agent`**.

Cursor shape (placeholder only):

```json
{
  "mcpServers": {
    "places-agent": {
      "url": "https://places.agent-mate.ai/mcp",
      "headers": {
        "Authorization": "Bearer <paste_plaintext_caller_api_key>"
      }
    }
  }
}
```

---

## 8. Reverse-proxy extras

| Feature | places-agent | what2eat / where2play |
| --- | --- | --- |
| WebSockets | **On** | On if the app uses WS; otherwise off is fine |
| SSE / streaming | **Required:** `proxy_buffering off`; `proxy_read_timeout` / `proxy_send_timeout` ≥ **300s** (Cursor `/mcp`, ChatBox `/sse` + `/messages`) | Only if the app streams |
| Custom Locations | **None** to a second container. Whole host → `places-agent:3000`. | None |
| After stack Recreate | **Save** the NPM host even if fields are unchanged (release-bot ADR-003) | Same for that host |
| Body size | Default OK unless admin later adds large uploads | Default OK |

kb-agent’s Custom Locations table is **not** the places-agent pattern.

---

## 9. Smoke checklist

Order: volume/migrate healthy → container healthy → DNS → NPM → public smoke. Do not expect public HTTPS before the container is up.

### places-agent (first wave)

- [ ] `GET https://places.agent-mate.ai/v1/health` (and `/health`) → `{ "agent": "places-agent", "ok": true }` (not NPM 502 / Default Site)
- [ ] `https://places.agent-mate.ai/` loads operator home (not Default Site)
- [ ] `https://places.agent-mate.ai/login` — sign in as `admin` (email `me@ethanhuang.com`); empty seed → `/set-password`; then admin usable
- [ ] Public register remains unavailable
- [ ] Admin: issue a caller API key (copy plaintext once; do not commit it)
- [ ] Cursor: `https://places.agent-mate.ai/mcp` + Bearer lists tools
- [ ] ChatBox: `https://places.agent-mate.ai/sse` + `Authorization=Bearer …` Test succeeds (**not** `/mcp`)
- [ ] Restaurant search: Bearer → `POST /v1/search_restaurants` (and/or MCP) returns structured result or explicit skip/error (empty list OK; never silent empty success; no `fixture_` in live)
- [ ] Place search: Bearer → `POST /v1/search_places` (same honesty)
- [ ] Timed itinerary: Bearer → `POST /v1/plan_itinerary` with `detail:"timed"` — days present; not `timed_no_places` when vendors work
- [ ] Chat: Bearer → `POST /v1/chat` (HTTP-only; **not** an MCP tool)
- [ ] Cursor `/mcp` **tools/list** = six tools only (no chat, no enrich)
- [ ] `PLACES_VENDOR_MODE=live`; `GOOGLE_DIRECT_FORCE_FAIL` and `QUANZIL_MODE` **unset**
- [ ] Homepage `200` is **not** the MCP gate; health + `/mcp` or `/sse` must pass
- [ ] HTML IOC: public `/` body must **not** contain `data:text/javascript;base64` (ClickFix lesson)
- [ ] Spot-check ≥1 existing app on 野草云3: `https://kb.agent-mate.ai` or `https://mypoke.trade` or `https://hcp.agent-mate.ai`

### what2eat (second wave)

- [ ] `https://what2eat.food/` loads
- [ ] BFF can call places-agent `search_restaurants` with server-side `PLACES_AGENT_CALLER_KEY`
- [ ] Spot-check ≥1 existing app (include `https://places.agent-mate.ai/v1/health`)

### where2play (third wave)

- [ ] `https://where2play.place/` loads
- [ ] BFF can call `search_places` and `plan_itinerary` (MVP-2)
- [ ] Spot-check ≥1 existing app

---

## 10. Isolation checklist

Follow `0.2.release-bot/knowledge/09-isolation-safety.md`. Fill before **each** first deploy (and when adding ports/domains).

### Shared (every stack in this family)

- [ ] Will **not** recreate `portainer_network`
- [ ] Will **not** edit other NPM hosts / other DNS records
- [ ] Will **not** migrate Aliyun DBs belonging to `kb_agent` / `mypoke_trade_prod` / `media_marketing` / `hca` / `what2eat`
- [ ] Post-deploy spot-check of ≥1 existing app planned
- [ ] Rollback plan touches **only this stack’s** `IMAGE_TAG` (and prompt ids for places-agent)

### places-agent

- [ ] Stack name `places-agent` free on Portainer
- [ ] Container name `places-agent` free
- [ ] Aliyun Postgres DB **`places_agent`** created and dedicated (not `what2eat` / kb / mypoke)
- [ ] **Proposed** host port **`3007`** free (`ss -lntup | grep ':3007'`); if taken, pick another unused port and record it — do not take `3001`–`3003` / `3006` / `3200`–`3203` / `6333` / `6335` / `6336`
- [ ] Domain `places.agent-mate.ai` not used by another NPM host
- [ ] No fourth `places-admin` stack created

### what2eat / where2play (when deploying those)

- [ ] Stack `what2eat` / `where2play` free
- [ ] Containers `what2eat-web` / `where2play-web` free
- [ ] **Proposed** host ports **`3004`** / **`3005`** free
- [ ] what2eat: Postgres DB **`what2eat`** on Aliyun — not shared with kb/mypoke/media/hca
- [ ] Domains `what2eat.food` / `where2play.place` not used by another NPM host

---

## 11. Ops caveats

| Topic | Rule |
| --- | --- |
| `IMAGE_TAG` | Published GHCR tag only (`sha` / `v*` / `latest`). **Never** git branch `main` (release-bot ADR-002). Confirm the tag exists on Packages before Portainer Update. |
| Portainer pull | “Update” often does **not** re-pull `latest`. Recreate + Pull, or pin sha. |
| NPM Save after recreate | After replacing the places-agent container, **Save** the NPM host `places.agent-mate.ai` even without field changes (release-bot ADR-003). Homepage 200 ≠ MCP healthy. |
| Postgres backup | Image rollback does **not** roll back Aliyun `places_agent`. Backup/snapshot that database before schema migrate. |
| Seed | `admin` / `me@ethanhuang.com`. No password in the image. Prod: `BOOTSTRAP_ADMIN_PASSWORD` one-time then unset, or empty hash → `/set-password`. Public register off. |
| Secrets | Rotate anything pasted into chat or screenshots. Never commit `.env`, `.keys`, or caller-key plaintext. |
| GMaps Worker | Not a VPS/Portainer stack. Do not invent a `gmaps-mcp` service on `portainer_network`. |
| Next standalone uploads | Do not serve post-boot `public/` from standalone (release-bot ADR-001). Thin apps: no volume until a spec needs uploads. |
| npm mirror | Aliyun `https://registry.npmmirror.com` for installs **on 野草云3 if any**. Prod images build in GitHub Actions, not on the VPS. |
| ClickFix / HTML inject | Keep Next patched. Probe HTML for `data:text/javascript;base64` before trusting public DNS (release-bot ADR-004). Grey cloud until LE OK. Do not treat homepage 200 as MCP healthy. |
| ChatBox after outage | Clients may need reconnect or app restart after a prolonged agent recreate. |
| LLM rollback | Pin `PROMPT_ID` / `GLOSSARY_ID` / `CATALOG_PACK` with `IMAGE_TAG`. No model registry. |
| Entrypoint | `node server.ts`. Not `next start`. Not a second MCP process. |
| Prod vendor mode | `PLACES_VENDOR_MODE=live`. Never `GOOGLE_DIRECT_FORCE_FAIL`, `QUANZIL_MODE=fixture`, or `DEV_ADMIN_PASSWORD`. |
| Isolation | Do not restart other stacks to “fix” this one. |

---

## 12. Release step map (for release-bot)

**Order:** artifacts exist (not blocked) → isolation gate → pull/deploy containers → DNS → NPM → smoke. Do not expect public HTTPS before containers are healthy. **First wave = places-agent only.**

### 12.1 First wave — `places-agent`

| Step | This plan’s answer |
| --- | --- |
| 0 Preflight | Repo `ethanhuangcst/places.agent-mate.ai`; stack `places-agent`; domain `places.agent-mate.ai`; image `ghcr.io/ethanhuangcst/places.agent-mate.ai/agent:<IMAGE_TAG>`; **proposed** host `3007→3000`; Postgres db `places_agent` on Aliyun. **Stop if § Blockers still missing.** |
| 0b Isolation | §10 + live inventory (`hcp` / `mypoke` / `kb-agent` / reserved media **3003**) |
| 1 Compose | App-repo `docker-compose.prod.yml` (skeleton in §4). Image-only; external `portainer_network`. |
| 2 CI → GHCR | `.github/workflows/ghcr.yml`. Tag = published sha / `v*` / `latest`, not `main`. |
| 3 Env | §5 places-agent table; `.env.production.example`. Secrets only in Portainer. Include `OPENAI_*`, map keys, `GMAPS_MCP_*`, `TRIPADVISOR_*`, `OPEN_METEO_*`, `PROMPT_ID` / `GLOSSARY_ID` / `CATALOG_PACK`. `PLACES_VENDOR_MODE=live`. Do not set `GOOGLE_DIRECT_FORCE_FAIL` or `QUANZIL_MODE`. |
| 4 DB | External Postgres **`places_agent`** on `101.132.156.250`; migrate + seed **on boot**. Do not use `what2eat`. Backup db before schema-changing deploys. |
| 5 Portainer | Stack `places-agent`; container `places-agent`; **no** SQLite volume. Recreate + Pull. |
| 6 DNS | Zone `agent-mate.ai`, record `places` → A `38.55.192.140`. Grey cloud until LE OK. |
| 7 NPM | Forward `http://places-agent:3000`. SSE extras §8. **No** Custom Locations to a second container. **Save** after recreate. |
| 8 Smoke | §9 places-agent (admin, issue key, health, Cursor `/mcp` six tools, ChatBox `/sse`, `search_restaurants` + `search_places` + timed `plan_itinerary` + `/v1/chat`, existing-app spot-check) |

### 12.2 Second wave — `what2eat`

Gate: places-agent MVP-1 can serve `search_restaurants`; what2eat app runtime + Docker/CI exist.

| Step | This plan’s answer |
| --- | --- |
| 0 Preflight | Repo `ethanhuangcst/what2eat.food`; stack `what2eat`; domain `what2eat.food`; image `…/what2eat.food/web:<tag>`; **proposed** `3004→3000`. **Stop if app-repo blockers remain.** |
| 0b Isolation | §10 what2eat + do not touch `places-agent` NPM/DNS except spot-check |
| 1–2 | Child-repo compose + `ghcr.yml` |
| 3 Env | `PUBLIC_BASE_URL`, `SESSION_SECRET`, `DATABASE_URL`, `PLACES_AGENT_BASE_URL`, `PLACES_AGENT_CALLER_KEY` (issued on places-agent). No map keys. No `OPENAI_*` on what2eat. |
| 4 DB | External Postgres **`what2eat`** on `101.132.156.250`; migrate on boot; backup before schema change |
| 5 Portainer | Stack `what2eat`; container `what2eat-web`; **no** app volume; host debug **`3004→3000`** |
| 6 DNS | Apex `what2eat.food` → `38.55.192.140` (mypoke pattern); grey until LE OK |
| 7 NPM | `http://what2eat-web:3000` |
| 8 Smoke | §9 what2eat + existing-app spot-check |

### 12.3 Third wave — `where2play`

Gate: places-agent **accepted** MVP-2 (`search_places` + `plan_itinerary` in code 2026-08-19); where2play app runtime + Docker/CI exist.

| Step | This plan’s answer |
| --- | --- |
| 0 Preflight | Repo `ethanhuangcst/where2play.places`; stack `where2play`; domain `where2play.place`; image `…/where2play.places/web:<tag>`; **proposed** `3005→3000` |
| 0b Isolation | §10 where2play |
| 1–2 | Child-repo compose + `ghcr.yml` |
| 3 Env | Same thin pattern as what2eat; `PUBLIC_BASE_URL=https://where2play.place`; local `PORT=3030`; prod host **`3005→3000`** |
| 4 DB | N/A |
| 5 Portainer | Stack `where2play`; container `where2play-web` |
| 6 DNS | Apex `where2play.place` → `38.55.192.140` |
| 7 NPM | `http://where2play-web:3000` |
| 8 Smoke | §9 where2play + existing-app spot-check |

---

## Cross-links

| Doc | Role |
| --- | --- |
| [`2.architecture.md`](./2.architecture.md) §10 | Locked topology (three stacks; one process on places-agent) |
| [`3.tech-specs.md`](./3.tech-specs.md) | Env codes, Quanzil, vendors |
| [`adr/ADR-009-deploy-option-1.md`](./adr/ADR-009-deploy-option-1.md) | Three services / images / stacks |
| [`adr/ADR-012-admin-ui-on-agent.md`](./adr/ADR-012-admin-ui-on-agent.md) | Admin UI in the agent image |
| [`adr/ADR-013-caller-agent-id.md`](./adr/ADR-013-caller-agent-id.md) | Caller-visible id `places-agent` |
| [`adr/ADR-015-sqlite-prisma.md`](./adr/ADR-015-sqlite-prisma.md) | SQLite on volume (superseded by ADR-025) |
| [`adr/ADR-025-places-agent-postgres-prisma.md`](./adr/ADR-025-places-agent-postgres-prisma.md) | PostgreSQL for places-agent |
| [`adr/ADR-023-what2eat-postgres-prisma.md`](./adr/ADR-023-what2eat-postgres-prisma.md) | PostgreSQL for what2eat |
| [`adr/ADR-016-custom-http-server.md`](./adr/ADR-016-custom-http-server.md) | `server.ts` entry |
| [`adr/ADR-017-gmaps-mcp-fallback.md`](./adr/ADR-017-gmaps-mcp-fallback.md) | Worker not on VPS |
| [`adr/ADR-018-mvp-by-capability.md`](./adr/ADR-018-mvp-by-capability.md) | Phased go-live |
| [`adr/ADR-019-http-first-user-test-automation.md`](./adr/ADR-019-http-first-user-test-automation.md) | ChatBox TC-C not a prod smoke substitute |
| [`adr/ADR-020-http-only-chat-and-enrich.md`](./adr/ADR-020-http-only-chat-and-enrich.md) | Chat and enrich HTTP-only |
| [`adr/ADR-021-live-vendor-no-fixture.md`](./adr/ADR-021-live-vendor-no-fixture.md) | Live mode must not serve fixture |
| [`adr/ADR-022-timed-itinerary.md`](./adr/ADR-022-timed-itinerary.md) | `detail: timed` |
| [`adr/ADR-024-quality-gates-typescript-7.md`](./adr/ADR-024-quality-gates-typescript-7.md) | Local `make quality`; not in prod image |
| [`knowledge/handbook.md`](./knowledge/handbook.md) §7 | Deploy facts |
| release-bot `knowledge/03-semi-auto-release.md` | Step order |
| release-bot `knowledge/09-isolation-safety.md` | Isolation |
| [`../2.what2eat/2eat-specs/6.deployment-plan.md`](../2.what2eat/2eat-specs/6.deployment-plan.md) | what2eat stack detail |
| release-bot `svr_hk_vps_3/hk_vps_3_setting.md` | Live inventory (refresh if stale) |
